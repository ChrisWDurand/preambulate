# preambulate — graph memory (local only, never commit)
memory.db/
graph_export.json
.preambulate_id
.preambulate_sync_state.json

# Python build artifacts
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
