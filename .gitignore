# preambulate — graph memory (local only, never commit)
memory.db/
graph_export.json
.preambulate_id
.preambulate_sync_state.json

# Local only (not for distribution)
CLAUDE.md
archive/
interface/
docs/

# Secrets and credentials — vacuously contextual; never commit
*.key
*.pem
*.cert
*.p12
*.pfx
.env
.env.*
*_api_key
*_secret

# Python build artifacts
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
